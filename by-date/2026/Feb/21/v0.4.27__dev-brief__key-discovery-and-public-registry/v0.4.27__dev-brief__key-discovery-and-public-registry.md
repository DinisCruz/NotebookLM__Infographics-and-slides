# Developer Brief: Key Discovery UI and Public Registry

**version** v0.4.12  
**date** 20 Feb 2026  
**classification** B3 as B1 / D1  
**from** Human (project lead)  
**to** Developer (lead), Designer, Architect  
**type** Developer brief — build spec (follows on from admin PKI key management page)  
**status** ⬅ CURRENT PRIORITY — This is the immediate next build.  

---

## Context

The admin PKI key management page is built and deployed. Two browsers, key generation, public key export/import, encrypt/decrypt — all working in production on the admin panel. This was achieved in ~24 hours from the investor meeting brief.

**This brief covers the next build: how do users discover and exchange public keys without manually copy-pasting PEM blobs?**

---

## What to Build

Two new features, both on the admin panel:

1. **Friendly Key Lookup** — find a public key by entering a human-readable name or code
2. **Public Key Registry** — a page listing published public keys that others can browse and download

Both are new pages in `/admin/`, following the same pattern as `pki.html`.

---

## Design Principle: No Plaintext on the Server

All data stored on the server is either:

- **Encrypted content** (PKI-encrypted blobs — the server cannot read them)
- **Tokens / Object IDs** (8-character hexadecimal values or full GUIDs/UUIDs)

**Folder names, key labels, user names — NONE of these are stored as strings on the server.** Everything is an Obj_Id or UUID. This is non-negotiable for two reasons:

1. **Security**: no metadata leakage. The server stores `a3f7c891` not `Deal-Alpha-Acquisition`. Traffic analysis reveals nothing.
2. **Injection prevention**: there are no strings to inject into. No SQL injection, no XSS via metadata, no path traversal. The server processes only tokens and encrypted blobs. The attack surface for injection is zero by design.

Friendly names exist only on the client side (in IndexedDB, mapped to Obj_Ids) or as encrypted metadata within PKI-encrypted blobs.

---

## Feature 1: Friendly Key Lookup

### The Concept

Instead of sharing a PEM blob, a user shares a short identifier. The recipient enters it, the server returns the public key.

```
Alice: "My SG/Send ID is dinis-cruz"
Bob:   goes to lookup page → types "dinis-cruz" → gets Alice's public key → imports it
```

### Server-Side Storage

The "friendly name" maps to a public key via an Obj_Id:

```
Registry entry (server-side):
{
  obj_id: "a3f7c891",              // 8-hex Obj_Id — the lookup token
  public_key_pem: "-----BEGIN...", // the public key (this IS public data)
  friendly_token: "dinis-cruz",    // stored as a hash, not plaintext
  created: "2026-02-20T10:00:00Z",
  updated: "2026-02-20T10:00:00Z",
  active: true
}
```

**Wait — the friendly name**: even "dinis-cruz" is metadata. Two options:

| Approach | How It Works | Trade-Off |
|---|---|---|
| **Hash the friendly name** | Server stores `sha256("dinis-cruz")`. Client sends the hash to look up. Server matches hash → returns public key. The actual name never touches the server. | Server can't enumerate names. Lookup requires knowing the exact name. |
| **Friendly name IS the Obj_Id** | The user's lookup code is just an Obj_Id like `a3f7c891` or a UUID. No human-readable name on the server at all. Client-side label maps Obj_Id → "dinis-cruz" in IndexedDB. | Less user-friendly but zero metadata. |
| **Hybrid** | Use a short alphanumeric code (e.g., `DC-7X4F`) as the lookup token. Not a meaningful name, but short enough to speak aloud. Server stores only the code. | Good balance — no metadata leakage, reasonably user-friendly. |

**Recommendation**: start with the hybrid approach. The lookup code is a short token (6–8 alphanumeric characters) that the server stores directly. It's not a name — it's an opaque identifier that happens to be short enough to read out loud or write on a business card. Client-side, the user can label it whatever they want in their local contacts.

### The Lookup Page: `/admin/key-lookup.html`

```
┌─────────────────────────────────────────────────────────────────┐
│  Admin  ›  Key Lookup                                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Find someone's public key                                      │
│                                                                 │
│  Enter their SG/Send code:                                      │
│  ┌──────────────────────────────────┐                           │
│  │ DC-7X4F                          │  [ Look Up ]              │
│  └──────────────────────────────────┘                           │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  ✓ Key found                                            │   │
│  │                                                          │   │
│  │  Code:        DC-7X4F                                    │   │
│  │  Fingerprint: sha256:3a7f...c891                         │   │
│  │  Algorithm:   RSA-OAEP 4096-bit                          │   │
│  │  Published:   20 Feb 2026, 10:00                         │   │
│  │                                                          │   │
│  │  ⚠ Verify this fingerprint with the key owner            │   │
│  │    through a separate channel before trusting it.        │   │
│  │                                                          │   │
│  │  Label this contact: [________________________]          │   │
│  │                                                          │   │
│  │  [Import to My Contacts]    [View Full Public Key]       │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### Workflow

```
1. User enters lookup code
2. Client sends GET /api/keys/{code}
3. Server returns: { obj_id, public_key_pem, fingerprint, created }
4. Client displays the key details
5. User optionally verifies fingerprint out-of-band
6. User enters a local label (stored in IndexedDB only)
7. User clicks "Import to My Contacts"
8. Public key imported into IndexedDB "contacts" store (same as pki.html)
```

### Publishing Your Key

On the existing `pki.html` page, add a **Publish** button to each key pair:

```
┌──────────────────────────────────────────────────────────────┐
│ 🔑  "My Work Key"                                           │
│     Created: 19 Feb 2026, 14:30                              │
│     Fingerprint: sha256:3a7f...c891                          │
│                                                              │
│     [Copy Public Key]  [Share via SG/Send]  [Delete]         │
│     [Publish to Registry]                                    │
└──────────────────────────────────────────────────────────────┘
```

Clicking "Publish to Registry":

```
1. Client sends POST /api/keys
   Body: { public_key_pem: "..." }
2. Server stores the key, generates a lookup code
3. Server returns: { code: "DC-7X4F", obj_id: "a3f7c891" }
4. Client shows:

   ┌─────────────────────────────────────────────┐
   │  ✓ Key published                            │
   │                                              │
   │  Your SG/Send code: DC-7X4F                  │
   │                                              │
   │  Share this code with anyone who wants       │
   │  to send you encrypted messages.             │
   │                                              │
   │  [Copy Code]  [Show as QR Code]              │
   └─────────────────────────────────────────────┘
```

### API Endpoints

| Endpoint | Method | Purpose |
|---|---|---|
| `POST /api/keys` | POST | Publish a public key → returns lookup code |
| `GET /api/keys/{code}` | GET | Retrieve a public key by lookup code |
| `DELETE /api/keys/{code}` | DELETE | Unpublish a key (requires authentication) |
| `GET /api/keys` | GET | List all published keys (for the registry page — see Feature 2) |

---

## Feature 2: Public Key Registry Page

### The Concept

A browsable page showing all published public keys for this server instance. For private deployments (like the investor's branded instance), only authorised users can access it. For public instances, it's open.

This is the beginning of a key server — analogous to keys.openpgp.org but per-deployment.

### The Registry Page: `/admin/key-registry.html`

```
┌─────────────────────────────────────────────────────────────────┐
│  Admin  ›  Public Key Registry                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Published keys on this server                                  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Code: DC-7X4F                                          │   │
│  │  Fingerprint: sha256:3a7f...c891                        │   │
│  │  Algorithm: RSA-OAEP 4096-bit                           │   │
│  │  Published: 20 Feb 2026, 10:00                          │   │
│  │  [Import]  [View Key]                                   │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │  Code: AB-3M9K                                          │   │
│  │  Fingerprint: sha256:8b2e...f443                        │   │
│  │  Algorithm: RSA-OAEP 4096-bit                           │   │
│  │  Published: 20 Feb 2026, 11:30                          │   │
│  │  [Import]  [View Key]                                   │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │  Code: FX-2R7P                                          │   │
│  │  Fingerprint: sha256:c44a...b118                        │   │
│  │  Algorithm: ECDH P-256                                  │   │
│  │  Published: 20 Feb 2026, 12:15                          │   │
│  │  [Import]  [View Key]                                   │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ⓘ All keys shown are PUBLIC keys. They are safe to download.  │
│    Always verify fingerprints with the key owner.               │
│                                                                 │
│  Certificate Transparency: All key registrations and changes    │
│  are logged in an append-only audit trail. [View Log]           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### Certificate Transparency Log

**All key operations are logged in an append-only log** visible to all users of the platform:

```
┌──────────────────────────────────────────────────────────────────┐
│  Key Transparency Log                                           │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  2026-02-20 12:15  KEY_PUBLISHED  FX-2R7P  sha256:c44a...b118  │
│  2026-02-20 11:30  KEY_PUBLISHED  AB-3M9K  sha256:8b2e...f443  │
│  2026-02-20 10:00  KEY_PUBLISHED  DC-7X4F  sha256:3a7f...c891  │
│                                                                  │
│  Log integrity: sha256:9f3a...2d71 (hash of all entries)        │
│  Previous hash: sha256:0000...0000 (genesis)                    │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

This is the transparency mechanism that detects if the server starts serving attacker-controlled keys. Any user can audit the log. Changes are visible. Deletions are visible. Additions are visible.

---

## File Structure

```
/admin/
├── index.html              ← existing (add nav links)
├── pki.html                ← existing (add "Publish" button)
├── key-lookup.html         ← NEW
├── key-lookup.js           ← NEW
├── key-registry.html       ← NEW
├── key-registry.js         ← NEW
└── shared/
    └── pki-common.js       ← shared utilities (PEM parse, fingerprint, IndexedDB)
```

Extract shared utilities from `pki.js` into `pki-common.js` so all three pages can reuse them.

---

## Acceptance Criteria

| # | Criterion | How to Verify |
|---|---|---|
| 1 | Can publish a public key from pki.html | Click Publish → get a lookup code |
| 2 | Can look up a key by code | Enter code on key-lookup.html → key displayed |
| 3 | Can import a looked-up key to contacts | Click Import → key appears in pki.html contacts |
| 4 | Registry page shows all published keys | Visit key-registry.html → all published keys listed |
| 5 | Can import a key from registry | Click Import on registry → key in contacts |
| 6 | Lookup code is 6–8 alphanumeric characters | Verify format on publish |
| 7 | No plaintext names stored on server | Inspect server storage — only Obj_Ids, PEM keys, timestamps |
| 8 | Transparency log records all key operations | Publish/unpublish → entry appears in log |
| 9 | Unpublishing a key works | Delete from registry → lookup returns 404, log shows removal |
| 10 | QR code for lookup code works | Click QR → scannable code shown |

---

This document is released under the Creative Commons Attribution 4.0 International licence (CC BY 4.0). You are free to share and adapt this material for any purpose, including commercially, as long as you give appropriate credit.
